{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Portfolio website — React + Node.js + PostgreSQL",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c cyan,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "db:migrate": "npm run db:migrate --prefix backend",
    "docker:up": "docker compose up --build",
    "docker:up:bg": "docker compose up --build -d",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f",
    "docker:db": "docker compose up postgres -d",
    "lint": "npm run lint --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "npm": "^5.10.0"
  }
}
